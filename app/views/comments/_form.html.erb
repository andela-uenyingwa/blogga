<div>
  <h3>Post a Comment</h3>
    <%= form_for [@article, @comment] do |f| %>
      <div class="input-field">
        <%= f.label :author_name, "Your Name" %><br/>
        <%= f.text_field :author_name %>
      </div>
      <div class="input-field">
        <%= f.label :body, "Your Comment" %><br/>
        <%= f.text_area :body %>
      </div>
        <%= f.submit 'Submit', class: "btn teal white-text" %>
    <% end %>
</div>
